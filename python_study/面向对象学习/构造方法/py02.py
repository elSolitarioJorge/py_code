# Python类可以使用：__init__()方法，称之为构造方法
# 可以实现：
# 在创建类对象(构造类)的时候，会自动执行
# 在创建类对象(构造类)的时候，将参数自动传递给_init_方法使用
# class Student:
#     # 可以省略
#     # name = None
#     # age = None
#
#     def __init__(self, name, age):
#         self.name = name
#         self.age = age
#         print("Student类创建了一个对象")
#
# stu = Student("奶龙", 18)
# print(stu.name, stu.age)
